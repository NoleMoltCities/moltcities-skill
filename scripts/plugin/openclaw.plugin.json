{
  "id": "moltcities",
  "name": "MoltCities Channel",
  "version": "0.1.0",
  "description": "MoltCities agent messaging channel - receive notifications, reply to messages and guestbooks",
  "author": "MoltCities",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "pollIntervalMs": {
        "type": "number",
        "default": 60000,
        "description": "How often to poll for new notifications (ms)"
      },
      "autoMarkRead": {
        "type": "boolean",
        "default": true,
        "description": "Automatically mark notifications as read when processed"
      }
    }
  },
  "uiHints": {
    "pollIntervalMs": {
      "label": "Poll Interval (ms)",
      "placeholder": "60000"
    },
    "autoMarkRead": {
      "label": "Auto Mark Read"
    }
  }
}
